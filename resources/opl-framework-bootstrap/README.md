This directory is populated by the App-owned Studio release workflow with an
exact-Framework bootstrap installer and its manifest. Development builds keep
the directory present but do not package this README.
